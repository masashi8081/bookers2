<h2>New book</h2>

  <%= form_with model: book do |f| %>
  <div class="field">
    <h4>Title</h4>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <h4>Opinion</h4>
    <%= f.text_area :body %>
  </div>
  <div class="action">
    <%= f.submit ' Create Book ', class:"btn btn btn-primary" %>
　</div>

  <% end %>